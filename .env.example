# ─── Claude Bridge Configuration ───────────────────────────────────────────
#
# Authentication (pick ONE):
#
# Option 1: Anthropic API key (from console.anthropic.com)
# ANTHROPIC_AUTH_TOKEN=sk-ant-api03-xxxxx
#
# Option 2: Claude CLI OAuth token (from ~/.claude/credentials.json)
# CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-xxxxx
#
# ───────────────────────────────────────────────────────────────────────────

# Server port (internal, Docker maps this externally)
PORT=8080

# Default model when none specified in request
DEFAULT_MODEL=claude-sonnet-4-5-20250929

# Anthropic API base URL (rarely need to change)
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# Max request body size (increase for large image payloads)
# REQUEST_BODY_LIMIT=10mb

# Logging: debug, info, warn, error
LOG_LEVEL=info
