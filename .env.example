# ─── Claude Bridge Configuration ──────────────────────────────────────────────

# Server port
PORT=8080

# Default Claude model
DEFAULT_MODEL=claude-sonnet-4-5-20250929

# Logging: info, debug
LOG_LEVEL=info

# CLI timeout in milliseconds (default: 5 minutes)
CLI_TIMEOUT_MS=300000

# Max concurrent CLI processes
MAX_CONCURRENT=10

# ─── Authentication (choose one) ─────────────────────────────────────────────

# Option 1: OAuth token (recommended for Max/Pro subscribers)
# Generate with: claude setup-token
CLAUDE_CODE_OAUTH_TOKEN=

# Option 2: Anthropic API key (uses API credits, not subscription)
# Get from: https://console.anthropic.com
ANTHROPIC_API_KEY=
